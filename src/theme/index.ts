import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1a1a27",
    },
    secondary: {
      main: "#007190",
    },
    error: {
      main: "#ff5252",
    },
  },
});

export default theme;
